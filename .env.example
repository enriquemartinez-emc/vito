# Copy to .env.local and fill in. For local development, run `npx supabase start`
# and copy the printed values directly (or run `npx supabase status`).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
